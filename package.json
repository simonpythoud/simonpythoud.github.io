{
  "name": "simonpythoud-com",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "particles.js": "^2.0.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10",
    "styled-components": "^2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -rf build/* . && rm -rf build",
    "test": "react-scripts test --env=jsdom"
  },
  "devDependencies": {
    "standard": "^10.0.2"
  }
}
